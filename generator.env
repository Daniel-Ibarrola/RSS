# Config for cap generator program. Do not modify CONFIG and SERVICE
CONFIG=prod
SERVICE=cap
# API user and password must be the same that the one used in api.env
API_URL=http://localhost:5000
API_USER=triton
API_PASSWORD=dog
CLIENT_IP=localhost
CLIENT_PORT=1234
